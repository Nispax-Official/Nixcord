{
  "name": "@nixcord/extension",
  "version": "1.0.0",
  "description": "Nixcord browser extension for Chrome and Firefox",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.21.4",
    "typescript": "^5.4.5"
  }
}
